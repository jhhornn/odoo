name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn
      - run: corepack enable
      - run: yarn install --frozen-lockfile
      - run: npm run build
      - run: node node_modules/eslint/bin/eslint.js "{src,test}/**/*.ts"
      - run: npm test -- --runInBand
      - run: npm run package:smoke
